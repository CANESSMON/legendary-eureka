# Job Portal Environment Variables
# Set to "true" to load mock/seed job data for development/demo
# Set to "false" to start with a clean slate (only employer-posted jobs show)
VITE_USE_MOCK_DATA=false

# API Base URL for backend services
# Leave empty for production build to use relative /api paths (via Nginx reverse proxy)
VITE_API_BASE_URL=
